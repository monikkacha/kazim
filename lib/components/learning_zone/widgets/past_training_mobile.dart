import 'package:flutter/material.dart';

class PastTrainingMobile extends StatelessWidget {
  const PastTrainingMobile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
        decoration: BoxDecoration(color: Colors.red),
        child: Center(child: Text("Past Training")));
  }
}
